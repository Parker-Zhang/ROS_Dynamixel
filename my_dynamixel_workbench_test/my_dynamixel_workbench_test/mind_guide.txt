catkin_make -DCATKIN_WHITELIST_PACKAGES=" 你的包名"

2020.2.27
目前需要解决的问题：
动态设置舵机的id，以及目标位置
计划采用的方法：
通过srv的方法改变舵机的id；srv的话就对应的一个回调函数，这个时候通过回调函数修改变量值即可，可以定义一个类或者结构体来保存
通过topic的方法改变舵机的目标位置，模仿turtlesim类型

master执行的代码就是：
不断发送舵机id号以及其对应的电流大小

master的职能就是读取舵机的数值，以及给舵机发送电流大小指令，然后各个舵机的节点都去订阅master发布的数值大小
各个节点会向周围的舵机节点发送

my_dxl 节点启动时需要能够设置其id值

舵机的id就默认为0 1 2依次排序

今日的计划：
1.查看turtlesim的源代码
2.my_dxl 节点启动时需要能够设置其id值
3.ros节点启动的时候如何调用配置文件
4.ros实现节点的参数动态更新

2020.2.29
实现了启动时加载配置文件

今日的计划：
1.学习配置文件的格式书写，要求能够设置舵机转角、速度、电流等的最大值、最小值(下午的任务)
2.搭建master-node的框架，如何搭建呢？
3.再继续跑一下官方给的demo，学习一下源码，明确方向

需求：
通过rqt_plot工具打印ros话题信息的曲线。 实现，进一步在launch文件中添加rqt启动项，然后选择默认的曲线
需要通过service来改变目标位置
如何一次性发布所有舵机的数据，一是单个舵机的msg文件，另外一个是list.msg文件

有两种方法，一是通过service来写入目标的电流，这样可以实现节点的通讯；
另外一种方法是通过动态参数来调整目标位置
两个方法都可以尝试一下